/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['pic1.zhimg.com', 'pica.zhimg.com', 'www.helloimg.com']
  }
};

module.exports = nextConfig;
